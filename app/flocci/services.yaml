apiVersion: v1
kind: Service
metadata:
  name: floci-service
spec:
  type: NodePort
  selector:
    app: floci
  ports:
    - name: api
      port: 4566
      targetPort: 4566
      # Opcional: Se quiser fixar uma porta no seu Node (entre 30000-32767)
      # nodePort: 30566